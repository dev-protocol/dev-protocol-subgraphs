{
  "name": "staking-amount-l2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dev-protocol/staking-amount-l2",
    "create-local": "graph create --node http://localhost:8020/ dev-protocol/staking-amount-l2",
    "remove-local": "graph remove --node http://localhost:8020/ dev-protocol/staking-amount-l2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dev-protocol/staking-amount-l2",
    "prepare:matic": "mustache config/matic.json subgraph.template.yaml > subgraph.yaml",
    "prepare:arbitrum-one": "mustache config/arbitrum-one.json subgraph.template.yaml > subgraph.yaml"
  }
}