{
  "name": "STokensManager",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ cryptodogg/STokensManager",
    "create-local": "graph create --node http://localhost:8020/ cryptodogg/STokensManager",
    "remove-local": "graph remove --node http://localhost:8020/ cryptodogg/STokensManager",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cryptodogg/STokensManager",
    "prepare:matic": "mustache config/matic.json subgraph.template.yaml > subgraph.yaml",
    "prepare:arbitrum-one": "mustache config/arbitrum-one.json subgraph.template.yaml > subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0",
    "mustache": "^3.1.0"
  }
}